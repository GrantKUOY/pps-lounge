{
  "name": "pps-lounge-v2",
  "private": true,
  "type": "module",
  "scripts": {
    "extract": "node scripts/extract-live-data.mjs .baseline/index.production.html pps-records.js data/manual-additions.json",
    "validate": "node scripts/validate-data.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:ui": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
